# Place this exact-match location inside the HTTPS FILSCORE server block.
# Ollama and the fallback FastAPI process remain bound to localhost.
location = /local-ai/page-assistant {
    client_max_body_size 16k;

    proxy_pass http://127.0.0.1:5001/ai/page-assistant/public;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_connect_timeout 5s;
    proxy_read_timeout 120s;
    proxy_send_timeout 120s;
}
